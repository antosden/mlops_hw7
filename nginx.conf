events {}

http {

    upstream ml_backend {
        server ml-service-v1:8000 weight=9;
        server ml-service-v2:8000 weight=1;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://ml_backend;
        }
    }
}